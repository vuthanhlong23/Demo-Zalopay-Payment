h1 Users

form(action="/users/search", method="GET")
  input(type="text", name="q", value=question)
  button Search

ul
  each user in userslist
    li
      span= user.name
      span &nbsp
      a(href='/users/'+ user.id) View

a(href="/users/post" ) Create new user